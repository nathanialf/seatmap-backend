describe('Test Setup', () => {
  it('should pass a basic test', () => {
    expect(true).toBe(true);
  });

  it('should handle basic arithmetic', () => {
    expect(1 + 1).toBe(2);
  });
}); 